{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate --no-hints && npx prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
